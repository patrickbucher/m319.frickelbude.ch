+++
date = '2026-01-04T20:56:54+01:00'
title = 'Python'
weight = 10
+++

Bearbeite die folgenden Aufgaben **ohne** Python-Interpreter, d.h. auf Papier.

## Aufgabe 1: Initialisierung

Initialisiere folgende Variablen mit dem angegebenen Wert:

1. `yes` und `no` mit den beiden entsprechenden Wahrheitswerten
2. `today` mit dem heutigen Wochentag als Zeichenkette
3. `year`, `month` und `day` als Ganzzahlen für die heutige Datumsangabe
4. `ratio` als Ergebnis der Divisionen aus der aktuellen Tagesangabe und der Anzahl Tagen im aktuellen Monat

## Aufgabe 2: Ausdrücke

1. Schreibe einen Ausdruck der abhängig von der Variable `year` zurückgibt, ob es sich dabei um ein Schaltjahr handelt.
2. Schreibe die folgenden Formeln als Python-Ausdrücke hin. (Die Quadratwurzel wird mit `sqrt()` berechnet.)

## Aufgabe 3: Formeln

```math

$$ t = t_0 \times p_{\alpha} \times + p_{\beta}^{n} $$

$$ \text{BMI}=\frac{\text{Gewicht}}{\text{Grösse}^2} $$

$$c=\sqrt{(a^2+b^2)}$$

$$p_{d} = p_{f} - p_{f} * \frac{d_r}{100}$$

$$ U = 2 \pi r $$

$$ A = \pi r^2 $$

$$ N = \frac{P_e}{P_m} \times 5 + 1 $$

$$F=C \times \frac{9}{5} + 32$$

$$C=(F - 32) \times \frac{5}{9}$$

$$ax^2+bx+c=0$$

$$D=b^2-4ac$$

$$ x_{1,2} = \frac{-b \pm \sqrt{D}}{2a} $$

```
## Aufgabe 4: Formatierte Ausgabe